# YOLOv5 🚀 by Ultralytics, GPL-3.0 license

# Parameters
nc: 3  # number of classes
depth_multiple: 1.0  # model depth multiple
width_multiple: 1.0  # layer channel multiple
anchors:
  - [10,13, 16,30, 33,23]  # P3/8
  - [30,61, 62,45, 59,119]  # P4/16
  - [116,90, 156,198, 373,326]  # P5/32

# YOLOv5 v6.0 backbone
backbone:
  # [from, number, module, args]
  [
    # rgb stream P1-P3
    [-1, 1, Conv, [64, 6, 2, 2]],  # 0-P1/2
    [-1, 1, Conv, [128, 3, 2]],  # 1-P2/4
    [-1, 3, C3, [128]],  # 2-P2/4
    [-1, 1, Conv, [256, 3, 2]],  # 3-P3/8
    [-1, 6, C3, [256]],  # 4-P3/8
    # ir stream P1-P3
    [-4, 1, Conv, [64, 6, 2, 2]],  # 5-P1/2
    [-1, 1, Conv, [128, 3, 2]],  # 6-P2/4
    [-1, 3, C3, [128]],  # 7-P2/4
    [-1, 1, Conv, [256, 3, 2]],  # 8-P3/8
    [-1, 6, C3, [256]],  # 9-P3/8
    # P3 fusion
    # rgb
    [4, 1, HEBlock, [256]], # 10-P3/8
    [-1, 1, WeightFusionParam, [256]], # 11-P3/8 rgb intra
    [10, 1, RCAB, [256]], # 12-P3/8
    [[4,12], 1, Add, [256]], # 13-P3/8
    [-1, 1, WeightFusionParam, [256]], # 14-P3/8 ir inter
    # ir
    [9, 1, HEBlock, [256]], # 15- /8
    [-1, 1, WeightFusionParam, [256]], # 16-P3/8 ir intra
    [10, 1, RCAB, [256]], # 17-P3/8
    [[9,17], 1, Add, [256]], # 18-P3/8
    [-1, 1, WeightFusionParam, [256]], # 19-P3/8 rgb inter
    # fusion
    [[19,4,11], 1, Add, [256]], # 20-P3/8  rgb
    [[14,9,16], 1, Add, [256]], # 21-P3/8  ir


    # rgb stream P4
    [20, 1, Conv, [512, 3, 2]],  # 22-P4/16
    [-1, 9, C3, [512]],  # 23-P4/16
    # ir stream P4
    [21, 1, Conv, [512, 3, 2]],  # 24-P4/16
    [-1, 9, C3, [512]],  # 25-P4/16
    # P4 fusion
    # rgb
    [23, 1, HEBlock, [512]], # 26-P4/16
    [-1, 1, WeightFusionParam, [512]], # 27-P4/16 rgb intra
    [26, 1, RCAB, [512]], # 28-P4/16
    [[23,28], 1, Add, [512]], # 29-P4/16
    [-1, 1, WeightFusionParam, [512]], # 30-P4/16 ir inter
    # ir
    [25, 1, HEBlock, [512]], # 31-P4/16
    [-1, 1, WeightFusionParam, [512]], # 32-P4/16 ir intra
    [31, 1, RCAB, [512]], # 33-P4/16
    [[25,31], 1, Add, [512]], # 34-P4/16
    [-1, 1, WeightFusionParam, [512]], # 35-P4/16 rgb inter
    # fusion
    [[35,23,27], 1, Add, [512]], # 36-P4/16  rgb
    [[30,25,32], 1, Add, [512]], # 37-P4/16  ir


    # rgb stream P5
    [36, 1, Conv, [1024, 3, 2]],  # 38-P5/32
    [-1, 3, C3, [1024]],  # 39-P5/32
    [-1, 1, SPPF, [1024, 5]],  # 40-P5/32
    # ir stream P5
    [37, 1, Conv, [1024, 3, 2]],  # 41-P5/32
    [-1, 3, C3, [1024]],  # 42-P5/32
    [-1, 1, SPPF, [1024, 5]],  # 43-P5/32
    # P5 fusion
    # rgb
    [40, 1, HEBlock, [1024]], # 44-P5/32
    [-1, 1, WeightFusionParam, [1024]], #45-P5/32 rgb intra
    [44, 1, RCAB, [1024]], # 46-P5/32
    [[40,46], 1, Add, [1024]], # 47-P5/32
    [-1, 1, WeightFusionParam, [1024]], # 48-P5/32 ir inter
    # ir
    [43, 1, HEBlock, [1024]], # 49-P5/32
    [-1, 1, WeightFusionParam, [1024]], # 50-P5/32 ir intra
    [49, 1, RCAB, [1024]], # 51-P5/32
    [[43,51], 1, Add, [1024]], # 52-P5/32
    [-1, 1, WeightFusionParam, [1024]], # 53-P5/32 rgb inter
    # fusion
    [[53,40,45], 1, Add, [1024]], # 54-P5/32  rgb
    [[48,43,50], 1, Add, [1024]], # 55-P5/32  ir


    # fusion to head
    [20, 1, WeightFusionParam, [256]], # 56-P3/8 rgb
    [21, 1, WeightFusionParam, [256]], # 57-P3/8 ir
    [[56,57], 1, Add, [256]], # 58-P3/8

    [36, 1, WeightFusionParam, [512]], # 59-P4/16 rgb
    [37, 1, WeightFusionParam, [512]], # 60-P4/16 ir
    [[59,60], 1, Add, [512]], # 61-P4/16

    [54, 1, WeightFusionParam, [1024]], # 62-P5/32 rgb
    [55, 1, WeightFusionParam, [1024]], # 63-P5/32 ir
    [[62,63], 1, Add, [1024]], # 64-P5/32
  ]

# YOLOv5 v6.0 head
head:
  [[-1, 1, Conv, [512, 1, 1]],  # 65
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 66
   [[-1, 61], 1, Concat, [1]],  # 67 cat backbone P4
   [-1, 3, C3, [512, False]],  # 68

   [-1, 1, Conv, [256, 1, 1]],  # 69
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 70
   [[-1, 58], 1, Concat, [1]],  # 71 cat backbone P3
   [-1, 3, C3, [256, False]],  # 72 (P3/8-small)

   [-1, 1, Conv, [256, 3, 2]],  # 73
   [[-1, 69], 1, Concat, [1]],  # 74 cat head P4
   [-1, 3, C3, [512, False]],  # 75 (P4/16-medium)

   [-1, 1, Conv, [512, 3, 2]],  # 76
   [[-1, 65], 1, Concat, [1]],  # 77 cat head P5
   [-1, 3, C3, [1024, False]],  # 78 (P5/32-large)

   [[72, 75, 78], 1, Detect, [nc, anchors]],  # 79 Detect(P3, P4, P5)
  ]
